ARCADE_API_KEY=arc_proj_YOUR_KEY_HERE
ARCADE_USER_ID=you@arcade.dev
ENGINE_URL=https://api.arcade.dev
ENGINE_HOST=api.arcade.dev
CATE_WEBHOOK_URL=http://localhost:8888
GATEWAY_SLUG=demo-gateway
ORG_ID=
PROJECT_ID=
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
WATCH_SENDER=
# Set DEMO_QUIET=
# Where the agent writes: Linear team name and Slack channel (workspace is set
# by the ARCADE_USER_ID's OAuth grants — use a demo user pointed at dev workspaces)
LINEAR_TEAM=DEMO
SLACK_CHANNEL=demo-engineering1 to skip Linear/Slack/GDoc during rehearsals (sandbox + PR only)
DEMO_QUIET=
# Where the agent writes: Linear team name and Slack channel (workspace is set
# by the ARCADE_USER_ID's OAuth grants — use a demo user pointed at dev workspaces)
LINEAR_TEAM=DEMO
SLACK_CHANNEL=demo-engineering
# Repo the agent clones and fixes (this repo; the bug lives in buggy-api/)
DEMO_REPO_URL=https://github.com/arcadeai-labs/daytona-background-agents
